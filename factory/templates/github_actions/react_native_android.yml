# AI Factory Pipeline v5.6 - React Native Android Build Workflow
# Referenced by: §4.5.1 CLI Build Paths, §2.7 Execution Mode Manager
# Auto-generated by factory/pipeline/s4_build.py

name: React Native Android Build

on:
  workflow_dispatch:
    inputs:
      build_id:
        description: 'Pipeline build identifier'
        required: true
      variant:
        description: 'Build variant (debug/release)'
        required: false
        default: 'release'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: |
          cd android
          npm ci

      - name: Create release keystore
        if: inputs.variant == 'release'
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/release.keystore
          echo "KEYSTORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}" >> $GITHUB_ENV
          echo "KEY_ALIAS=${{ secrets.KEY_ALIAS }}" >> $GITHUB_ENV
          echo "KEY_PASSWORD=${{ secrets.KEY_PASSWORD }}" >> $GITHUB_ENV

      - name: Build APK/AAB
        run: |
          cd android
          if [ "${{ inputs.variant }}" == "release" ]; then
            ./gradlew bundleRelease assembleRelease
          else
            ./gradlew assembleDebug
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-${{ inputs.variant }}-${{ inputs.build_id }}
          path: |
            android/app/build/outputs/apk/**/*.apk
            android/app/build/outputs/bundle/**/*.aab
          retention-days: 90

      - name: Report build status
        if: always()
        run: |
          echo "BUILD_STATUS=${{ job.status }}" >> $GITHUB_ENV
          echo "Build completed with status: ${{ job.status }}"
