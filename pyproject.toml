[project]
name = "ai-factory-pipeline"
version = "5.6.0"
description = "AI Factory Pipeline v5.6 — Autonomous App Factory"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "strict"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
